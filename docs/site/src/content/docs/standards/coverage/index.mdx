---
title: Code Coverage
description: Test coverage report generated by PHPUnit on every commit.
---

import { LinkCard } from '@astrojs/starlight/components';

![Coverage badge](https://raw.githubusercontent.com/phpdftk/phpdftk/_coverage/latest/coverage-badge.svg)

Coverage is measured across every PHPUnit suite (`core`, `writer`, `reader`,
`toolkit`, `conformance`, plus support packages) using `pcov`.

<LinkCard
	title="Open the Coverage Report"
	description="Per-file line coverage for the entire monorepo."
	href={`${import.meta.env.BASE_URL}coverage/`}
/>

## How it's generated

Run locally with `composer coverage`. The HTML report lands in
`build/coverage/html/`. CI publishes per-commit and per-tag snapshots to the
`_coverage` orphan branch, and bundles the latest HTML into this site.
