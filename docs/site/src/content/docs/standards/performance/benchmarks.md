---
title: Benchmarks
description: Auto-generated benchmark results vs. TCPDF, FPDF, mPDF, Dompdf, smalot/pdfparser, and setasign/fpdi.
---

This page is auto-generated by CI from the most recent main-branch run of
`composer benchmark`. The CI `docs` job overwrites this file before the
Astro build, replacing it with the latest results from the `_benchmarks`
branch.

If you're seeing this placeholder on a deployed site, the embed step
either failed or the `_benchmarks` branch hasn't been populated yet.
Run `composer benchmark` locally to generate `docs/generated/benchmarks.md`,
or check the CI logs for the `benchmarks` job.
