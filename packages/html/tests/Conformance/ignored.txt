# html5lib-tests deferral ledger
#
# Per the project-decisions strict-conformance posture (see
# docs/plans/html-and-svg.md), every test that doesn't yet pass lives here
# with a one-line reason. CI fails if a passing test stops passing OR if a
# previously-failing test starts passing without being removed from this list.
#
# Phase 1 covers the common path; this list captures the WHATWG spec edges
# the parser doesn't yet implement (adoption-agency variants, foreign-
# content cdata, template + plaintext quirks, entity-tokenizer edge cases,
# and a handful of feature gaps).
